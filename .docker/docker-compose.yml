version: '3.6'
services:
  
  kritner-website-web:
    image: ${DOCKER_REGISTRY}/kritnerwebsite
    ports:
      - 5000:5000
    networks:
      - frontend
    environment:
      - PORT=5000
    restart: always
    container_name: kritnerwebsite_web
  
networks:
  frontend: